[user]
    name = ning
    email = ningyuan.sg@gmail.com
[alias]
    alias = config --get-regexp alias
    amend = commit --amend
    amend-ne = commit --amend --no-edit
    commit = commit -v
    graph = log --graph --oneline --decorate
    log = log --oneline --stat
    log-long = log
    ls = ls-files
    nuke = reset --hard
    s = status -bs
[core]
    editor = "vim"
[diff]
	tool = vimdiff
[difftool]
	prompt = false
